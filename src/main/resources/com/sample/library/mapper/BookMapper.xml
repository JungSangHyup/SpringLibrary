<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sample.library.mapper.BookMapper">
	<insert id="save">
	<![CDATA[
        INSERT INTO book (num, mid, subject, content, readcount, reg_date, ipaddr, re_ref, re_lev, re_seq)
        VALUES (#{num}, #{mid}, #{subject}, #{content}, #{readcount}, #{regDate}, #{ipaddr}, #{reRef}, #{reLev},
                #{reSeq})
        ]]>
	</insert>
</mapper>